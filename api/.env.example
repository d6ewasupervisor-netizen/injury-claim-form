RESEND_API_KEY=
# Comma-separated operations inboxes (defaults to tyson.gauthier@retailodyssey.com if unset)
# CLAIMS_OPS_TO=
# Hop count for Express trust proxy (default 1). Try 2 if behind Cloudflare + Railway.
# TRUST_PROXY_HOPS=1
PORT=3000
# Comma-separated browser origins allowed to call the API (GitHub Pages + local dev)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://YOUR_USER.github.io
